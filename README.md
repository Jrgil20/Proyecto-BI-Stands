# Proyecto-BI-Stands

### ETL (ExpoEventos)
Encuentra los artefactos iniciales para los procesos ETL en la carpeta `etl/`.

- `etl/README_ETL.md`: instrucciones y pasos rápidos para ejecutar los jobs.
- `etl/kettle.properties`: archivo de variables (NO subir a repositorio con credenciales reales).

Sigue `etl/README_ETL.md` para continuar la implementación y ejecutar los jobs de ejemplo.
